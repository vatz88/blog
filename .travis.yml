language: node_js
node_js:
  - "lts/*"

install:
  - npm install # make sure to install the deps when Travis runs.
after_success:
  - npm run lh -- --perf=80 --seo=90 --a11y=90 --bp=85 https://blog.vatz88.in
